{
  "variables": {
    "box_basename": "centos-8.3",
    "build_directory": "builds",
    "http_directory": "{{template_dir}}/http",
    "cpus": "2",
    "disk_size": "65536",
    "headless": "",
    "http_proxy": "{{env `http_proxy`}}",
    "https_proxy": "{{env `https_proxy`}}",
    "hyperv_generation": "1",
    "hyperv_switch": "{{env `hyperv_switch`}}",
    "iso_checksum": "aaf9d4b3071c16dbbda01dfe06085e5d0fdac76df323e3bbe87cce4318052247",
    "iso_name": "CentOS-8.3.2011-x86_64-dvd1.iso",
    "ks_path": "ks.cfg",
    "memory": "2048",
    "mirror": "http://mirror.vcu.edu",
    "mirror_directory": "pub/gnu_linux/centos/8.3.2011/isos/x86_64",
    "name": "centos-8.3",
    "no_proxy": "{{env `no_proxy`}}",
    "template": "SERIOps Fukuoka M1",
    "version": "TIMESTAMP",
    "key_file": "id_ecdsa",
    "home": "{{ env `HOME` }}",
    "vga": "794",
    "boot_command": "<up><wait><tab> inst.text vga={{ user `vga` }} inst.ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{user `ks_path`}}<enter><wait>",
    "seri_source": "/Users/wim.vandijck/Documents/SailPoint/SERI/seri"
  },
  "builders": [
    {
      "boot_command": "{{ user `boot_command` }}",
      "boot_wait": "5s",
      "cpus": "{{ user `cpus` }}",
      "disk_size": "{{user `disk_size`}}",
      "guest_additions_path": "VBoxGuestAdditions_{{.Version}}.iso",
      "guest_additions_url": "{{ user `guest_additions_url` }}",
      "guest_os_type": "RedHat_64",
      "hard_drive_interface": "sata",
      "headless": "{{ user `headless` }}",
      "http_directory": "{{user `http_directory`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_url": "{{user `mirror`}}/{{user `mirror_directory`}}/{{user `iso_name`}}",
      "memory": "{{ user `memory` }}",
      "output_directory": "{{ user `build_directory` }}/packer-{{user `template`}}-virtualbox",
      "shutdown_command": "echo 'sailpoint' | sudo -S /sbin/halt -h -p",
      "ssh_password": "Sailp0!nt",
      "ssh_port": 22,
      "ssh_timeout": "10000s",
      "ssh_username": "sailpoint",
      "type": "virtualbox-iso",
      "virtualbox_version_file": ".vbox_version",
      "vm_name": "{{ user `template` }}"
    },
    {
      "boot_command": "{{ user `boot_command` }}",
      "boot_wait": "5s",
      "cpus": "{{ user `cpus` }}",
      "disk_size": "{{user `disk_size`}}",
      "guest_os_type": "centos-64",
      "headless": "{{ user `headless` }}",
      "http_directory": "{{user `http_directory`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_url": "{{user `mirror`}}/{{user `mirror_directory`}}/{{user `iso_name`}}",
      "memory": "{{ user `memory` }}",
      "output_directory": "{{ user `build_directory` }}/packer-{{user `template`}}-vmware",
      "shutdown_command": "echo 'sailpoint' | sudo -S /sbin/halt -h -p",
      "ssh_password": "Sailp0!nt",
      "ssh_port": 22,
      "ssh_timeout": "10000s",
      "ssh_username": "sailpoint",
      "tools_upload_flavor": "linux",
      "type": "vmware-iso",
      "vm_name": "{{ user `template` }}",
      "vmx_data": {
        "cpuid.coresPerSocket": "1"
      },
      "vmx_remove_ethernet_interfaces": true
    },
    {
      "boot_command": [
        "<up><wait><tab> text ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{user `ks_path`}}<enter><wait>"
      ],
      "boot_wait": "10s",
      "disk_size": "{{user `disk_size`}}",
      "guest_os_type": "centos",
      "http_directory": "{{user `http_directory`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_url": "{{user `mirror`}}/{{user `mirror_directory`}}/{{user `iso_name`}}",
      "output_directory": "{{ user `build_directory` }}/packer-{{user `template`}}-parallels",
      "parallels_tools_flavor": "lin",
      "memory": "{{ user `memory` }}",
      "cpus": "{{ user `cpus` }}",
      "prlctl_version_file": ".prlctl_version",
      "shutdown_command": "echo 'sailpoint' | sudo -S /sbin/halt -h -p",
      "ssh_password": "Sailp0!nt",
      "ssh_port": 22,
      "ssh_username": "sailpoint",
      "ssh_wait_timeout": "10000s",
      "type": "parallels-iso",
      "vm_name": "{{ user `template` }}"
    },
    {
      "boot_command": [
        "<wait5><tab> text ks=hd:fd0:/ks.cfg<enter><wait5><esc>"
      ],
      "boot_wait": "10s",
      "cpus": "{{ user `cpus` }}",
      "disk_size": "{{user `disk_size`}}",
      "floppy_files": [
        "{{user `http_directory`}}/{{user `ks_path`}}"
      ],
      "generation": "{{user `hyperv_generation`}}",
      "guest_additions_mode": "disable",
      "http_directory": "{{user `http_directory`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_url": "{{user `mirror`}}/{{user `mirror_directory`}}/{{user `iso_name`}}",
      "memory": "{{ user `memory` }}",
      "output_directory": "{{ user `build_directory` }}/packer-{{user `template`}}-hyperv",
      "shutdown_command": "echo 'sailpoint' | sudo -S /sbin/halt -h -p",
      "ssh_password": "Sailp0!nt",
      "ssh_port": 22,
      "ssh_timeout": "10000s",
      "ssh_username": "sailpoint",
      "switch_name": "{{ user `hyperv_switch`}}",
      "type": "hyperv-iso",
      "vm_name": "{{ user `template` }}"
    },
    {
      "boot_command": [
        "<up><wait><tab> text ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{user `ks_path`}}<enter><wait>"
      ],
      "boot_wait": "10s",
      "memory": "{{ user `memory` }}",
      "cpus": "{{ user `cpus` }}",
      "disk_size": "{{user `disk_size`}}",
      "headless": "{{ user `headless` }}",
      "http_directory": "{{user `http_directory`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_url": "{{user `mirror`}}/{{user `mirror_directory`}}/{{user `iso_name`}}",
      "output_directory": "{{ user `build_directory` }}/packer-{{user `template`}}-qemu",
      "shutdown_command": "echo 'sailpoint'|sudo -S /sbin/halt -h -p",
      "ssh_password": "Sailp0!nt",
      "ssh_port": 22,
      "ssh_username": "sailpoint",
      "ssh_wait_timeout": "10000s",
      "type": "qemu",
      "vm_name": "{{ user `template` }}"
    }
  ],
  "provisioners": [
    {
      "environment_vars": [
        "HOME_DIR=/home/sailpoint",
        "http_proxy={{user `http_proxy`}}",
        "https_proxy={{user `https_proxy`}}",
        "no_proxy={{user `no_proxy`}}"
      ],
      "execute_command": "echo 'sailpoint' | {{.Vars}} sudo -S -E sh -eux '{{.Path}}'",
      "expect_disconnect": true,
      "scripts": [
        "update.sh",
        "ansible.sh",
        "motd.sh",
        "vagrant.sh",
        "parallels.sh",
        "cleanup.sh"
      ],
      "type": "shell"
    },
    {
      "type": "file",
      "source": "../ansible/vault-password-file",
      "destination": "/tmp/vault-password-file"
    },
    {
      "type": "ansible-local",
      "extra_arguments": [ "--vault-password-file /tmp/vault-password-file --extra-vars \"key_file=/tmp/{{ user `key_file` }}\"" ],
      "playbook_file": "../ansible/playbook.yml",
      "role_paths": [
        "../ansible/roles/identityiq",
        "../ansible/roles/init",
        "../ansible/roles/lvps.389ds_server",
        "../ansible/roles/robertdebock.bootstrap",
        "../ansible/roles/robertdebock.core_dependencies",
        "../ansible/roles/robertdebock.java",
        "../ansible/roles/robertdebock.mysql",
        "../ansible/roles/robertdebock.service",
        "../ansible/roles/robertdebock.tomcat",
        "../ansible/roles/robertdebock.update"
      ]
    },
    {
      "environment_vars": [
        "HOME_DIR=/home/sailpoint"
      ],
      "execute_command": "echo 'sailpoint' | {{.Vars}} sudo -S -E sh -eux '{{.Path}}'",
      "expect_disconnect": true,
      "scripts": [
        "minimize.sh"
      ],
      "type": "shell"
    }
  ],
  "post-processors": [
    {
      "output": "{{ user `build_directory` }}/{{user `box_basename`}}.{{.Provider}}.box",
      "keep_input_artifact":true,
      "type": "vagrant"
    }
  ]
}